import {StyleSheet} from 'react-native';

export const errors = StyleSheet.create({
    wrapper: {
        //flex: 1,
        alignItems: 'flex-end',
    },
    text: {
        fontSize: 12,
        color: 'red',
    }
});