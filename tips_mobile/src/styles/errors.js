import { StyleSheet } from 'react-native';

export const errors = StyleSheet.create({
  wrapper: {
    marginRight: 5,
    alignItems: 'flex-end',
  },
  text: {
    fontSize: 12,
    color: 'red',
  },
});
